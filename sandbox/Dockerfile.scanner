# Optional: Cisco AI Defense Skill Scanner, used by the scan-then-load gate.
# Build: docker build -f sandbox/Dockerfile.scanner -t asshack-scanner:latest .
FROM asshack-sandbox:latest

USER root
RUN pip install --no-cache-dir cisco-ai-skill-scanner
USER 65532:65532
CMD ["bash"]
